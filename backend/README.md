# Auto Parts Backend

Lightweight Express backend to serve the Auto Parts frontend. Fill `.env` values and run `npm install` then `npm run dev`.

Endpoints are mounted under `/api` to match the frontend's `REACT_APP_API_URL` default.
